export type {
  ChatMessage,
  CheckResult,
  UserProgress,
  WebviewMessage,
  ExtensionMessage,
} from '../../../src/protocol';
export type { Course, CourseListItem, Lesson, Document, Task, Resource } from '../../../src/types';